<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Chat</title>
  <link rel="stylesheet" href="/edit.css" />
</head>
<body>
  <main class="edit-container">
    <h2>Edit Chat</h2>
    <p>
      Chat ID: <b><%= chat._id %></b><br />
      Message sent from <strong><%= chat.from %></strong> to <strong><%= chat.to %></strong>
    </p>

    <form action="/chats/<%= chat._id %>?_method=PUT" method="POST">
      <label for="msg">Edit Message:</label>
      <textarea id="msg" name="msg" required><%= chat.msg %></textarea>

      <button type="submit">Update Chat</button>
    </form>
  </main>
</body>
</html>
